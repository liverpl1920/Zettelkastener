# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## フェーズ1: ステアリングとレビュー指摘の整理

- [x] ステアリングファイルを作成する
  - [x] `requirements.md` を作成する
  - [x] `design.md` を作成する
  - [x] `tasklist.md` を作成する

- [x] レビュー指摘事項を修正方針へ落とし込む
  - [x] 重複解消の single source of truth を決める
  - [x] high / medium / low 優先度の修正範囲を整理する

## フェーズ2: ドキュメント修正

- [x] `docs/functional-design.md` を修正する
  - [x] 誤字と garbled text を修正する
  - [x] `folgezettelId` の nullable 条件を明確化する

- [x] `docs/product-requirements.md` を修正する
  - [x] KPI測定方法とスマホ対応表現を明確化する
  - [x] リリースマイルストーンを追加する
  - [x] パフォーマンステスト基準を参照方式へ整理する

- [x] `docs/architecture.md` を修正する
  - [x] PWAのフェーズ表現を統一する
  - [x] Folgezettel採番アルゴリズムを参照方式へ整理する

- [x] `docs/development-guidelines.md` を修正する
  - [x] Reactコンポーネント規約を追加する
  - [x] 想定 npm scripts 一覧と IPC Handlers のテスト方針を補足する
  - [x] 誤字を修正する

- [x] `docs/glossary.md` を修正する
  - [x] 誤記を修正する
  - [x] 開発ツール用語を追加する
  - [x] 索引を更新する

- [x] `docs/repository-structure.md` を修正する
  - [x] workflow ファイル例を追加する
  - [x] Folgezettel 関連UIコンポーネント例を追加する

## フェーズ3: 品質チェックと修正

- [x] 修正結果を確認する
  - [x] 各指摘事項が解消されたことを差分で確認する
  - [x] ドキュメント間の参照関係が自然であることを確認する

## フェーズ4: ドキュメント更新

- [x] 実装後の振り返りを記録する

## フェーズ5: レビュー結果の追加是正

- [x] `docs/functional-design.md` のレビュー指摘を修正する
  - [x] `PromoteInput` の型定義を追加する
  - [x] `NoteService.getById()` の契約を明確化する

- [x] `docs/product-requirements.md` のレビュー指摘を修正する
  - [x] 機能2と機能11のスコープ境界を明記する

- [x] `docs/architecture.md` のレビュー指摘を修正する
  - [x] エラークラス定義の正本参照を明記する

- [x] `docs/glossary.md` のレビュー指摘を修正する
  - [x] 日本語索引の表記揺れを修正する

- [x] 修正後の整合性を確認する
  - [x] 指摘事項4件がすべて解消されていることを確認する
  - [x] 参照リンクと型名の整合性を確認する

## フェーズ6: 再レビュー指摘の最終是正

- [x] `docs/product-requirements.md` の未解消指摘を修正する
  - [x] 知識の生成KPIに補助指標を追加する
  - [x] リリースマイルストーンを現実的な時期へ更新する

- [x] `docs/functional-design.md` の未解消指摘を修正する
  - [x] `Index (MoC)` 作成のユースケースフローを追加する
  - [x] PWA詳細設計の切り出し方針を明記する

- [x] `docs/architecture.md` の未解消指摘を修正する
  - [x] Zustand Store の正規化方針を明記する

- [x] 最終確認を実施する
  - [x] 再レビューで残っていた指摘が解消されたことを確認する
  - [x] 追加修正が既存記述と矛盾しないことを確認する

---

## 実装後の振り返り

### 実装完了日
2026-03-08

### 計画と実績の差分

**計画と異なった点**:
- `functional-design.md` と `development-guidelines.md` に、レビューで明示されていなかった garbled text / typo も合わせて修正した
- Folgezettel採番アルゴリズムは `functional-design.md` を正本にし、`architecture.md` は責務記述へ縮約する方針にした
- 追加レビュー結果を受けて、`PromoteInput` の型定義補完、`getById()` 契約の明確化、P0/P1境界の追記、索引表記統一を同一ステアリング内で追補した
- 再レビュー後に残っていた低優先度指摘も同一ステアリング内で解消し、マイルストーン現実性・UC追加・Zustand状態方針まで補完した

**新たに必要になったタスク**:
- `docs/product-requirements.md` の P2 機能に重複ノート検出アルゴリズムの方向性を追記した
- レビューの低優先度指摘も同時に解消し、再レビューの手戻りを防ぐため
- 追加レビューで検出された4件の指摘を解消するため、フェーズ5を追加した
- 再レビューで残っていた3件の指摘を解消するため、フェーズ6を追加した

**技術的理由でスキップしたタスク**（該当する場合のみ）:
- なし

### 学んだこと

**技術的な学び**:
- 永続ドキュメントでは「高レベル要求」「詳細仕様」「実装規約」の責務を分けるだけで、重複と矛盾をかなり減らせる

**プロセス上の改善点**:
- ステアリングファイルで修正範囲を先に分類したことで、高優先度から低優先度まで漏れなく反映できた
- レビュー結果を一度で閉じ切るため、未解消指摘をドキュメントごとに再分類して最小差分で追補する進め方が有効だった

### 次回への改善提案
- 今後のドキュメント追加時も single source of truth を先に定義し、重複記載を避ける