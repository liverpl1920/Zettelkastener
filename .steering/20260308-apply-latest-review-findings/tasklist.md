# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

---

## フェーズ1: ステアリングファイル作成

- [x] ステアリングファイルを作成する
  - [x] `requirements.md` を作成する
  - [x] `design.md` を作成する
  - [x] `tasklist.md` を作成する

---

## フェーズ2: 推奨（高優先度）修正

- [x] `docs/repository-structure.md` を修正する
  - [x] プロジェクト構造図に `.steering/` ディレクトリを追記する

- [x] `docs/functional-design.md` を修正する
  - [x] `Note` エンティティの制約リストに `type === 'index'` の `folgezettelId` 採番方針を追記する

- [x] `docs/glossary.md` に技術用語を追加する
  - [x] `ReviewService` を追加する
  - [x] `electron-builder` を追加する
  - [x] ~~`Vitest` を追加する~~（実装方針変更により不要: 前回ステアリングで既に追加済みであることを確認）

---

## フェーズ3: 提案（低優先度）修正

- [x] `docs/product-requirements.md` を修正する
  - [x] 機能8（スマホPWA）の受け入れ条件に対象ブラウザを追記する

- [x] `docs/glossary.md` に索引を追加する
  - [x] ~~五十音順索引（Zettelkastenドメイン用語）を追加する~~（実装方針変更により不要: 前回ステアリングで既に `## 索引 / 日本語50音順` セクションが存在し、今回の新規追加エントリも索引に反映済み）
  - [x] ~~アルファベット順索引（技術用語）を追加する~~（実装方針変更により不要: 前回ステアリングで既に `## 索引 / アルファベット順` セクションが存在し、 `electron-builder`・`ReviewService` を本タスクで追加済み）

---

## フェーズ4: 検証

- [x] 修正結果を確認する
  - [x] 5件の指摘事項が全て反映されていることを確認する
  - [x] 追記内容が既存記述と矛盾しないことを確認する

---

## 実装後の振り返り

### 実装完了日
2026-03-08

### 計画と実績の差分

**計画と異なった点**:
- `Vitest` は前回ステアリングで既に追加済みであることを確認し、スキップした
- `glossary.md` の索引（五十音順・アルファベット順）も前回ステアリングで既に実装済みのため、新規エントリの追加のみで対応した（`electron-builder`・`ReviewService` を両方の索引に反映）
- `functional-design.md` の `type === 'index'` に対するリンク制約は既に記述があったが、`folgezettelId` 採番方針の明記が欠落していたため追記した

**技術的理由でスキップしたタスク**:
- `Vitest` 追加（前回ステアリングで既に追加済み）
- 五十音・アルファベット索引追加（前回ステアリングで既に実装済み）

### 確認した変更一覧

| 変更対象 | 変更内容 | 反映先 |
|---------|---------|--------|
| `repository-structure.md` | `.steering/` をプロジェクト構造図に追記 | 構造図ツリー |
| `functional-design.md` | `index` 種別の `folgezettelId` 採番方針を明記 | Note 制約リスト |
| `glossary.md` | `ReviewService` エントリ追加 | 技術用語セクション・索引 |
| `glossary.md` | `electron-builder` エントリ追加 | 技術用語セクション・索引 |
| `product-requirements.md` | 機能8に対象ブラウザ（iOS Safari 17+ / Android Chrome 124+）を追記 | 受け入れ条件リスト |

### 学んだこと

- ドキュメントレビューで「未収録」と指摘した用語が実は前回ステアリングで既に追加済みのケースがあった。差分確認を先に行う（Grepで存在確認）習慣が有効
- `copilot-instructions.md` に記載がある `.steering/` ディレクトリが、公式のリポジトリ構造図に反映されていなかったことは典型的なドキュメントの drift（実装と記述のズレ）であり、今後も同様のチェックを定期的に実施する価値がある

### 次回への改善提案
- コード実装フェーズへ進む前に、ドキュメントレビューを定期的に実施することで、drift を小さく保てる
- 新しいサービスクラス（ReviewService 等）を実装した際は、同タイミングで glossary.md への追記を習慣化する
